<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title><%= title %> — CID Operator</title>
    <style>
      body {
        font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI",
          sans-serif;
        margin: 0;
        padding: 0;
        background: #020617;
        color: #e5e7eb;
      }
      header {
        padding: 16px 24px;
        border-bottom: 1px solid #1f2937;
      }
      header h1 {
        font-size: 18px;
        margin: 0 0 4px;
      }
      header .sub {
        font-size: 13px;
        color: #9ca3af;
      }
      main {
        padding: 16px 24px 32px;
      }
      a {
        color: #38bdf8;
        text-decoration: none;
      }
      a:hover {
        text-decoration: underline;
      }
      table {
        width: 100%;
        border-collapse: collapse;
        font-size: 13px;
      }
      th,
      td {
        padding: 8px 10px;
        border-bottom: 1px solid #111827;
        text-align: left;
      }
      th {
        color: #9ca3af;
        font-weight: 500;
      }
      tbody tr:hover {
        background: #0f172a;
      }
      .empty {
        color: #6b7280;
        font-size: 14px;
        padding: 24px 0;
      }
    </style>
  </head>
  <body>
    <header>
      <h1><%= title %></h1>
      <div class="sub">
        <%= typeof segmentLabel !== "undefined" && segmentLabel ? segmentLabel : "All segments" %> · same “today” window as Operator Home (UTC calendar day, server time)
      </div>
      <%- include('../partials/operator-nav', { segmentQuery: typeof segmentQuery !== 'undefined' ? segmentQuery : '', segment: typeof segment !== 'undefined' ? segment : 'all' }) %>
    </header>
    <main>
      <% if (!rows || rows.length === 0) { %>
      <p class="empty">No rows for this metric in the current UTC day.</p>
      <% } else { %>
      <table>
        <thead>
          <tr>
            <% columns.forEach(function (col) { %>
            <th><%= col.label %></th>
            <% }); %>
          </tr>
        </thead>
        <tbody>
          <% rows.forEach(function (row) { %>
          <tr>
            <% columns.forEach(function (col) { %>
            <td>
              <% var v = row[col.key]; %>
              <% if (col.link === "quote_packet" && row.quote_id) { %>
              <a href="/operator/packet-builder/<%= row.quote_id %><%= typeof segmentQuery !== 'undefined' && segmentQuery ? segmentQuery : '' %>"><%= v != null ? v : "—" %></a>
              <% } else if (col.link === "quote_bind" && row.quote_id) { %>
              <a href="/operator/bind/<%= row.quote_id %><%= typeof segmentQuery !== 'undefined' && segmentQuery ? segmentQuery : '' %>"><%= v != null ? v : "—" %></a>
              <% } else if (col.link === "policy_docs" && row.policy_id) { %>
              <a href="/operator/policies/<%= row.policy_id %>/documents<%= typeof segmentQuery !== 'undefined' && segmentQuery ? segmentQuery : '' %>"><%= v != null ? v : "—" %></a>
              <% } else if (col.key === "packet_status" && v === "SENT") { %>
              <span style="display:inline-block;padding:2px 8px;border-radius:999px;font-size:11px;background:#064e3b;color:#bbf7d0;font-weight:600">SENT</span>
              <% } else { %>
              <%= v != null ? v : "—" %>
              <% } %>
            </td>
            <% }); %>
          </tr>
          <% }); %>
        </tbody>
      </table>
      <% } %>
    </main>
  </body>
</html>

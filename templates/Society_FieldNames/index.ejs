<!doctype html>
<html>
<head>
  <meta charset="utf-8"/>
  <title>Society – Supplemental Application</title>
  <style><%= styles %></style>
</head>
<body>
  <h1>Society – Supplemental Application (Bar/Tavern)</h1>

  <h2>Applicant</h2>
  <p><strong>Business Name:</strong> <%= data.applicant_name || data.namedInsured || "" %></p>
  <p><strong>Location:</strong>
    <%= [data.premise_address || data.location_address,
          data.premise_city || data.location_city,
          data.premise_state || data.location_state,
          data.premise_zip || data.location_zip].filter(Boolean).join(", ") %>
  </p>
  <p><strong>Phone:</strong> <%= data.business_phone || data.phone || "" %> |
     <strong>Email:</strong> <%= data.contact_email || data.email || "" %></p>

  <h2>Operations Mix</h2>
  <table class="table">
    <tr><th>Liquor %</th><th>Food %</th><th>Other %</th></tr>
    <tr>
      <td><%= data.liquor_percent || data.percentLiquor || "" %></td>
      <td><%= data.food_percent || data.percentFood || "" %></td>
      <td><%= data.other_percent || data.percentOther || "" %></td>
    </tr>
  </table>

  <h2>Hours & Entertainment</h2>
  <p><strong>Hours:</strong> <%= data.hours_of_operation || "" %></p>
  <p><strong>Entertainment:</strong> <%= data.entertainment_description || data.entertainment || "" %></p>

  <h2>Security & Controls</h2>
  <p><strong>Security Staff:</strong> <%= (data.security_staff ? "Yes" : (data.security_staff===false ? "No" : "")) %></p>
  <p><strong>ID Check at Door:</strong> <%= (data.id_checking ? "Yes" : (data.id_checking===false ? "No" : "")) %></p>
  <p><strong>TIPS/ServSafe:</strong> <%= (data.tips_training || data.servsafe) ? "Yes" : "" %></p>

  <h2>Loss History</h2>
  <div style="min-height:60px;border:1px solid #000;padding:6px;">
    <%= data.loss_history || data.prior_losses || "" %>
  </div>
</body>
</html>

